# ═════════════════════════════════════════════
# TEMPLATE — copie ce fichier en « .env » et remplis les valeurs.
# .env est IGNORÉ par Git (jamais committé). .env.example est committé.
# ═════════════════════════════════════════════

# ── Flask / App ──
# Génère une clé :  python -c "import secrets; print(secrets.token_hex(32))"
LIQUID_SECRET_KEY=remplace-par-une-cle-aleatoire-64-hex-min
FLASK_DEBUG=0
LIQUID_BASE_URL=http://localhost:5050

# ── SMTP Scaleway TEM (mêmes identifiants que fu19) ──
TEM_SMTP_HOST=smtp.tem.scw.cloud
TEM_SMTP_PORT=587
TEM_SMTP_USER=
TEM_SMTP_PASSWORD=
TEM_SMTP_FROM=no-reply@fu19.org
